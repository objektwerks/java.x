db.driver=${db.driver}
db.username=${db.username}
db.password=${db.password}
db.url=${db.url}
db.initial.size=${db.initial.size}
db.max.active=${db.max.active}

persistence.unit.name=${persistence.unit.name}
persistence.xml.location=${persistence.xml.location}

hibernate.autocommit=${hibernate.autocommit}
hibernate.hbm2ddl.auto=${hibernate.hbm2ddl.auto}
hibernate.dialect=${hibernate.dialect}
hibernate.jdbc.batch.size=${hibernate.jdbc.batch.size}
hibernate.show.sql=${hibernate.show.sql}
hibernate.default.schema=${hibernate.default.schema}

pooled.xsd.validator.is.enabled=${pooled.xsd.validator.is.enabled}
pooled.xsd.validator.pool.size=${pooled.xsd.validator.pool.size}
pooled.xslt.transformer.pool.size=${pooled.xslt.transformer.pool.size}


